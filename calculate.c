#include <stdio.h>
int main(){
    char operator;
    double num1, num2, result;

    printf("enter an operator (+, -, *, /): ");
    scanf("%c", &operator);

    printf("enter two numbers: ");
    scanf("%lf %lf", &num1, &num2);

}
